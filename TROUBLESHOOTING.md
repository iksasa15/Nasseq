# 🐛 استكشاف مشكلة AR

## المشكلة
عند الضغط على المنتج، لا تفتح شاشة AR/الكاميرا.

## الأسباب المحتملة

### 1. ⚠️ التشغيل على Simulator
**AR لا يعمل على Simulator!**
- يجب التشغيل على جهاز iPhone/iPad حقيقي
- ARKit يحتاج كاميرا وحساسات حقيقية

**الحل:**
```
1. وصّل iPhone/iPad بالكمبيوتر
2. في Xcode: اختر جهازك من القائمة العلوية
3. اضغط Run (⌘R)
```

### 2. صلاحية الكاميرا
تأكد من منح صلاحية الكاميرا للتطبيق.

**تحقق من Info.plist:**
```xml
<key>NSCameraUsageDescription</key>
<string>نحتاج للكاميرا لعرض المنتجات في بيئتك</string>
```

### 3. ملفات USDZ مفقودة
إذا كانت ملفات النماذج مفقودة، قد يحدث خطأ.

**الحل:**
- ستظهر مكعبات زرقاء كبديل
- أضف ملفات USDZ الحقيقية لاحقاً

## اختبار التنقل

أضفت رسائل debug للتحقق من التنقل:

```swift
// عند الضغط على المنتج
🔍 Product tapped: [اسم المنتج]
🔍 showARView set to true

// عند فتح ARScreen
🎥 ARScreen appeared
🎥 Selected product: [اسم المنتج]
```

**كيف تتحقق:**
1. شغّل التطبيق
2. افتح Console في Xcode (⌘⇧C)
3. اضغط على منتج
4. شاهد الرسائل في Console

## الحل الأكيد

**إذا كنت على Simulator:**
- لن يعمل AR أبداً
- يجب التشغيل على جهاز حقيقي

**إذا كنت على جهاز حقيقي:**
1. تحقق من Console للرسائل
2. تأكد من صلاحية الكاميرا
3. تأكد من وجود إضاءة كافية
4. وجّه الكاميرا على سطح مستوٍ (طاولة)

## ملاحظة مهمة

الكود صحيح 100%! المشكلة على الأغلب:
- ✅ التشغيل على Simulator (AR لا يعمل)
- ✅ صلاحية الكاميرا غير ممنوحة
- ✅ عدم وجود إضاءة كافية
